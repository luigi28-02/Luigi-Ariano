<!--
This is the login page through which
the user or business can register and log in
-->

<!doctype html>
<html lang="en" data-bs-theme="auto">
<head>
    <link rel="manifest" href="../public/manifest.json">
    <link href="../public/css/sign-in.css" rel="stylesheet">
    <link href="../css" rel="stylesheet">
    <link href="../public/node_modules/bootstrap/dist/css/bootstrap.min.css" rel="stylesheet">
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Plant Eaters</title>
    <style>

        @media (min-width: 768px) {
            .bd-placeholder-img-lg {
                font-size: 3.5rem;
            }
        }

        .b-example-divider {
            width: 100%;
            height: 3rem;
            background-color: rgba(0, 0, 0, .1);
            border: solid rgba(0, 0, 0, .15);
            border-width: 1px 0;
            box-shadow: inset 0 .5em 1.5em rgba(0, 0, 0, .1), inset 0 .125em .5em rgba(0, 0, 0, .15);
        }

        .b-example-vr {
            flex-shrink: 0;
            width: 1.5rem;
            height: 100vh;
        }

        .bi {
            vertical-align: -.125em;
            fill: currentColor;
        }

    </style>


</head>
<body class="d-flex align-items-center py-4 bg-body-tertiary">
<%if(typeof flag !="undefined" && flag){ %>
    <script>
        alert("Account created successfully");
    </script>

<% }else if(typeof flag !="undefined"){%>
    <script>
        alert("This Account already exists");
    </script>
<%}%>
<%if(typeof flagpassword !="undefined" && flagpassword){ %>
    <script>
        alert("Password Changed Successfully");
    </script>

<% }else if(typeof flagpassword !="undefined"){%>
    <script>
        alert("The email does not exist");
    </script>
<%}%>

<%if(typeof flagpassworderror !="undefined" && flagpassworderror){ %>
    <script>
       alert("Wrong Password");
    </script>

<% }%>


<main class="form-signin w-100 m-auto">
    <form action="/login" method="post">
        <a href='/'><img class="mb-4 img-fluid mx-auto d-block" src="../public/image/heart.png" alt="" width="150" height="150"></a>
        <h1 class="h3 mb-3 fw-normal text-center fw-bold fs-1">Plant Eaters</h1>
        <div class="form-floating">
            <input type="email" class="form-control" name="email" id="floatingInput" placeholder="name@example.com">
            <label for="floatingInput">Email address</label>
        </div>
        <div class="form-floating">
            <input type="password" class="form-control" name="password" id="floatingPassword" placeholder="Password">
            <label for="floatingPassword">Password</label>
        </div>
        <button class="btn btn-primary w-100 py-2" type="submit">Sign in</button>
        <a  href="/forgotpassword" class="mt-3 mb-3 text-body-secondary d-block">Forgot password?</a>
        <a  href="/newuser" class="mt-3 mb-3 text-body-secondary d-block">New user? Sign Up</a>
        <a  href="/newactivity" class="mt-3 mb-3 text-body-secondary">Register Your Company</a>
    </form>
</main>


<script>
    if('serviceWorker' in navigator){
        //Se non ci sono problemi lo registriamo e dopo aver fatto questo restituiamo una promessa che al suo
        //interno ha un messaggio per capire se è andato tutto bene
        navigator.serviceWorker.register('sw.js').then(console.log("from sw"))
    }
</script>
</body>
</html>
